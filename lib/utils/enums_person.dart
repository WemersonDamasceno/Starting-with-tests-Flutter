// enum StatusStudent {
//   aproved(message: "O aluno foi aprovado", color: Colors.green),
//   reproved(message: "O aluno foi reprovado", color: Colors.red),
//   willMakeRecovery(message: "O aluno fará a AF", color: Colors.orange);

//   const StatusStudent({required this.message,required this.color});

//   final String message;
//   final Color color;
// }

enum StatusStudent {
  aproved,
  reproved,
  willMakeRecovery,
}
